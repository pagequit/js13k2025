{
  "devDependencies": {
    "esbuild": "^0.25.9",
    "prettier": "^3.6.2"
  },
  "scripts": {
    "dev": "esbuild src/main.js --bundle --minify --platform=neutral --sourcemap --outdir=public --watch --servedir=public",
    "bundle": "esbuild src/main.js --bundle --minify --platform=neutral --outdir=public"
  }
}
